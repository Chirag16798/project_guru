import React from 'react'

const OnceHeader = (props) => {
    return (
        <div className='once'>
              <h1 className='bigHeading'>{props.heading}</h1>
        </div>
    )
}

export default OnceHeader
